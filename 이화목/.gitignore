# 원본 데이터는 git에 올리지 않는다 (용량 큼) - README의 데이터 출처 참고해 각자 다운로드
data/raw/

# 가상환경
venv/
.venv/

# Python 캐시
__pycache__/
*.pyc

# 주의 : eda_chart_seaborn.png / eda_chart_plotly.html / model.joblib / report.md는
# 팀 SPEC상 병합 담당자가 비교해야 하는 산출물이므로 여기서 무시하지 않고 git에 커밋한다.

# 환경변수 파일 (실제 값은 개인별로 다르므로 .env.example만 공유)
.env

# OS/에디터
.DS_Store
.vscode/
.idea/
